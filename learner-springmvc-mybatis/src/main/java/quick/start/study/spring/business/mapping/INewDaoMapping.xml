<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="quick.start.study.spring.business.dao.INewDao">

    <insert id="addNew" parameterType="quick.start.study.spring.business.entity.New">
        insert into new (newId,newTitle,newDetails,pushTime,pushPerson,delFlag,flowUpTop,thumbnails)
        values(#{newId},#{newTitle},#{newDetails},#{pushTime},#{pushPerson},#{delFlag},#{flowUpTop},#{thumbnails})
    </insert>

</mapper>